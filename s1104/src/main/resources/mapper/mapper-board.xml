<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.site.ex.mapper.BoardMapper">

    <!-- 전체회원 정보 -->
	<select id="selectList" resultType="com.site.ex.vo.BoardVo">
		select * from board
	</select>
	

</mapper>